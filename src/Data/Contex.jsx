// for data parsing or gatting
// parents le data lena or child kodena

// Context(warehouse)
// Provider (delivery)
// Consumer / (useContext(you))



import React from "react";
const AppContext = React.createContext();
export default AppContext;
